# PINNED DELIBERATELY -- do not loosen. This file is re-resolved by Streamlit Community Cloud
# on every rebuild, and leaving it unpinned is what silently changed the runtime under the app
# on 2026-09-01 (Python 3.14.7 / pandas 3.0.5 / numpy 2.5.2 arrived in the 18:34 UTC rebuild).
# These are exactly the versions that rebuild installed, pinned so the environment stops
# moving. The app targets Python 3.14, so pandas must stay on the 3.x line -- pandas 2.x
# publishes no cp314 wheels and would fail to install.
streamlit==1.63.0
pandas==3.0.5
numpy==2.5.2
openpyxl==3.1.5
simple-salesforce==1.12.10
xlsxwriter>=3.1,<4
requests==2.34.2
